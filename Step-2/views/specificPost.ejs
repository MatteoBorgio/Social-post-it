<!DOCTYPE html>
<html lang="it">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= post.name %> - Dettaglio Post</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .post-image-detail {
            max-height: 70vh;
            object-fit: contain;
            background-color: #000;
        }
    </style>
</head>
<body class="bg-light">

<nav class="navbar navbar-dark bg-primary shadow-sm mb-5">
    <div class="container">
        <a class="navbar-brand fw-bold" href="/gallery">Social Post-it</a>
    </div>
</nav>

<div class="container mb-5">
    <div class="row justify-content-center">
        <div class="col-lg-10">

            <div class="mb-3">
                <a href="/gallery" class="btn btn-outline-secondary btn-sm">
                    &larr; Torna alla Galleria
                </a>
            </div>

            <div class="card shadow border-0 overflow-hidden">

                <img src="<%= post.link %>"
                     class="img-fluid post-image-detail"
                     alt="<%= post.name %>">

                <div class="card-body p-4 p-md-5">
                    <div class="d-flex justify-content-between align-items-center mb-3">
                        <h1 class="h2 fw-bold mb-0 text-dark"><%= post.name %></h1>
                    </div>

                    <hr class="my-4">

                    <h5 class="text-muted fw-bold">Descrizione</h5>
                    <p class="lead text-secondary">
                        <%= post.description %>
                    </p>
                </div>
            </div>

        </div>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>